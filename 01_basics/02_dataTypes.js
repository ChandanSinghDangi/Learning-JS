
"use strict"; // this treats all the java script code as new version...

// alert(3 + 3); // works on browser only... not in nodejs


// Types of Primitive Data:-

// String → "Hello";

// Number → 42, 3.14;

// Boolean → true, false;

// Undefined → variable declared but not assigned

// Null → intentional empty value

// Symbol → unique identifier (Symbol('id'))

// BigInt → very large integers (123456789012345678901234567890n)



// Types of Non-Primitive Data:-

// Object → { name: "Chandan", age: 22 }

// Array → [1, 2, 3]

// Function → function greet() { return "Hello"; }

// Date, RegExp, etc. (all derived from Object)



// Types of :-

console.log(typeof null); // null is an object remember for interview question.
console.log(typeof undefined); // undefined will have type of undefined.















